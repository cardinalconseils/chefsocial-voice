import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "ChefSocial Voice - AI-Powered Content for Restaurants",
  description: "Transform your restaurant descriptions into engaging social media content with AI-powered voice technology.",
  keywords: "restaurant marketing, AI content, voice to text, social media, restaurant management",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
